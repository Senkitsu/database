<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>
        
        <div class="actions">
            <a href="/schedule/courses/add" class="btn btn-primary">Добавить дисциплину</a>
            <a href="/schedule" class="btn btn-secondary">Назад к расписанию</a>
        </div>

        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Название</th>
                        <th>Кафедра</th>
                        <th>Часы (лек/пр/лаб)</th>
                        <th>Занятий в расписании</th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    <% courses.forEach(course => { %>
                    <tr>
                        <td><%= course.name %></td>
                        <td><%= course.department_name %></td>
                        <td><%= course.hours_lecture %>/<%= course.hours_practice %>/<%= course.hours_lab %></td>
                        <td><%= course.schedule_count %></td>
                        <td class="actions">
                            <a href="/schedule/courses/<%= course.id %>/edit" class="btn btn-edit">Редактировать</a>
                            <form action="/schedule/courses/<%= course.id %>/delete" method="POST" class="inline-form">
                                <button type="submit" class="btn btn-delete" onclick="return confirm('Удалить дисциплину?')">Удалить</button>
                            </form>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>