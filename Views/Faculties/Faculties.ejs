<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>
        
        <div class="actions">
            <a href="/faculties/add" class="btn btn-primary">Добавить факультет</a>
        </div>

        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Название</th>
                        <th>Декан</th>
                        <th>Телефон</th>
                        <th>Кафедр</th>
                        <th>Групп</th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    <% faculties.forEach(faculty => { %>
                    <tr>
                        <td><%= faculty.name %></td>
                        <td><%= faculty.dean %></td>
                        <td><%= faculty.phone %></td>
                        <td><%= faculty.department_count %></td>
                        <td><%= faculty.group_count %></td>
                        <td class="actions">
                            <a href="/faculties/<%= faculty.id %>/edit" class="btn btn-edit">Редактировать</a>
                            <form action="/faculties/<%= faculty.id %>/delete" method="POST" class="inline-form">
                                <button type="submit" class="btn btn-delete" onclick="return confirm('Удалить факультет?')">Удалить</button>
                            </form>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>